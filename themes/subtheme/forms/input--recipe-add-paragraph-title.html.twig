{#
/**
 * @file
 * Default theme implementation for an 'input' #type form element.
 *
 * Available variables:
 * - attributes: A list of HTML attributes for the input element.
 * - children: Optional additional rendered elements.
 *
 * @see template_preprocess_input()
 *
 * @ingroup themeable
 */
#}
<input placeholder="Eg. Cake, or Frosting (if recipe has multiple parts)" data-name="Ingredient Section Title" {{ attributes.addClass("form-field-66-pc w-input") }} />{{ children }}
